@model IEnumerable<BusinessEntities.ProductEntity>

<ul>@if (Model != null)
                {
                    foreach (var item in Model)
                    {
                        <li>
					        <div class="info_song">
						        <h3>
							        <a href="/home/playmusic?id=@item.Id">@Html.DisplayFor(modelItem => item.Name)</a>
							        -
							        <a href="">@Html.DisplayFor(modelItem => item.ArtistProduct.StageName)</a>
						        </h3>
						        <span class="float-right">@Html.DisplayFor(modelItem => item.Views)</span>
						        <div class="box_song_action"></div>
					        </div>
				        </li>
                    }
                }
    </ul>